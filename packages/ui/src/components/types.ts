
export type ButtonProps = {
    variant?: string,
    text?: boolean,
    outline?: boolean,
    small?: boolean,
    large?: boolean,
    active?: boolean,
    block?: boolean,
    href?: string,
    disabled?: boolean,
};
