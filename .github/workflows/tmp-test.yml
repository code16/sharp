name: Test dispatch

on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test"
  deploy:
    needs: test
    uses: code16/sharp/.github/workflows/deploy.yml@main
    secrets: inherit

